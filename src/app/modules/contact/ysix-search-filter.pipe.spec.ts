import { YsixSearchFilterPipe } from './ysix-search-filter.pipe';

describe('YsixSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new YsixSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
